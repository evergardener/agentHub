# Worker 注册表（静态 MVP；动态发现见 §29）
agents:
  codex:
    enabled: true
    endpoint: http://127.0.0.1:8201
    skills: [coding, testing, devops]
  kimi:
    enabled: true
    endpoint: http://127.0.0.1:8202
    skills: [research, long_context, analysis]
  dsh:
    # Disabled until the adapter advertises verified native permission
    # enforcement. A live, ready development heartbeat may override this
    # static seed, but production-preflight still rejects DSH routing.
    enabled: false
    endpoint: http://127.0.0.1:8203
    skills: [coding, code_review, analysis, agent_collaboration]
