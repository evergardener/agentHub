# Worker 注册表（静态 MVP；动态发现见 §29）
agents:
  codex:
    enabled: true
    endpoint: http://127.0.0.1:8201
    skills: [coding, testing, devops]
  kimi:
    # Temporarily excluded from production routing after the 2026-08-20
    # real ACP gate returned HTTP 403 quota exhausted. Keep the adapter and
    # profile definitions for a later quota-backed requalification.
    enabled: false
    endpoint: http://127.0.0.1:8202
    skills: [research, long_context, analysis]
  dsh:
    # Production-eligible only through the Adapter-enforced standard preset,
    # native read-only sandbox, ask policy, and signed allowed-once receipts.
    enabled: true
    endpoint: http://127.0.0.1:8203
    skills: [coding, code_review, analysis, agent_collaboration]
